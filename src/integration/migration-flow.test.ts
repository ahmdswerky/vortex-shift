import { describe, it } from 'vitest'

describe.skip('integration/full migration flow', () => {
  it('migrates a compose project from source to destination Rocky container', async () => {
    // Integration environment setup required:
    // - two Rocky Linux containers/VMs
    // - docker-in-docker source runtime
    // - source CLI execution against destination
  })
})
